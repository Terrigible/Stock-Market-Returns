# Agent Instructions

## Python Environment

Before running any commands, ensure the virtual environment is activated:

**Windows:**
```bash
source .venv/Scripts/activate
```

**macOS/Linux:**
```bash
source .venv/bin/activate
```

## Code Formatting

After editing any Python file, run ruff format to ensure consistent code style:

```bash
ruff format .
```
